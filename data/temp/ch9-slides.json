[
  { "type": "cover", "title": "第九章", "subtitle": "上下文工程" },
  { "type": "content", "title": "什么是上下文工程", "content": "上下文工程（Context Engineering）关注的是\"在每一次模型调用前，如何以可复用、可度量、可演进的方式，拼装并优化输入上下文\"，从而提升正确性、鲁棒性与效率。\n\n所谓\"上下文\"，是指对 LLM 进行采样时所包含的那组 tokens。工程问题在于：在 LLM 的固有约束之下，优化这些 tokens 的效用，以便稳定地得到预期结果。\n\n关键转变：从\"如何写出有效提示\"到\"如何策划最优的信息集合\"。" },
  { "type": "content", "title": "上下文工程 vs 提示工程", "content": "提示工程（Prompt Engineering）：\n• 关注如何编写与组织指令以获得更优结果\n• 核心问题：系统提示的写法与结构化策略\n• 适用于单轮分类或文本生成场景\n\n上下文工程（Context Engineering）：\n• 在推理阶段策划与维护\"最优的信息集合（tokens）\"\n• 包含提示本身 + 进入上下文窗口的一切信息\n  - 系统指令、工具、MCP、外部数据、消息历史\n\n演进动力：\n智能体在更长的时间范围内、跨多次推理轮次地工作 → 需要能管理整个上下文状态的策略。\n\n核心挑战：从持续扩张的\"候选信息宇宙\"中，甄别哪些内容应当进入有限的上下文窗口。" },
  { "type": "content", "title": "为什么上下文工程重要", "content": "尽管模型速度越来越快、可处理数据规模越来越大，但我们观察到：\n\n上下文腐蚀（Context Rot）\n• 随着上下文窗口中的 tokens 增加，模型准确回忆信息的能力反而下降\n• 不同模型的退化曲线或许更平滑，但这一特征几乎在所有模型上出现\n\n注意力预算有限\n• Transformer 让每个 token 与所有 token 建立 (n²) 级别的注意力关系\n• 上下文增长 → 注意力被\"拉薄\" → 性能下降\n• 模型对\"全上下文依赖\"的经验更少、专门参数也更少\n\n位置编码插值等技术可以让模型处理更长的序列，但会牺牲部分对 token 位置的精确理解。\n\n结论：上下文必须被视作一种有限资源，有意识的上下文工程是构建强健智能体的必需品。" },
  { "type": "animation", "title": "上下文窗口可视化", "animation": "ch9-context-window" },
  { "type": "content", "title": "有效上下文的解剖学", "content": "在\"有限注意力预算\"的约束下，上下文工程的目标：用尽可能少但高信号密度的 tokens，最大化获得期望结果的概率。\n\n系统提示（System Prompt）\n• 语言清晰、直白，信息层级把握在\"刚刚好\"的高度\n• 常见误区：过度硬编码（脆弱 if-else）vs 过于空泛（缺少具体信号）\n• 建议：分区组织，追求\"最小必要信息集\"\n\n工具（Tools）\n• 定义智能体与信息/行动空间的契约\n• 职责单一、接口语义清晰、对错误鲁棒\n• 最小可行工具集（MVTS）原则\n\n示例（Few-shot）\n• 精选多样且典型的示例，直接画像\"期望行为\"\n• 好的示例胜过千言万语" },
  { "type": "concepts", "title": "上下文工程的三大策略", "items": [
    { "icon": "📦", "title": "压缩整合（Compaction）", "description": "当对话接近上下文上限时，对其进行高保真总结，并用该摘要重启一个新窗口。先优化召回（不遗漏关键信息），再优化精确度（剔除冗余）。" },
    { "icon": "📝", "title": "结构化笔记（Structured Note-taking）", "description": "也称\"智能体记忆\"。以固定频率将关键信息写入上下文外的持久化存储，后续按需拉回。以极低的上下文开销维持持久状态与依赖关系。" },
    { "icon": "👥", "title": "子代理架构（Sub-agent Architectures）", "description": "主代理负责高层规划，多个专长子代理在\"干净的上下文窗口\"中各自深挖。庞杂的搜索上下文留在子代理内部，主代理专注于整合与推理。" }
  ] },
  { "type": "content", "title": "GSSC 流水线", "content": "ContextBuilder 的核心是 GSSC 流水线，将上下文构建过程分解为四个清晰的阶段：\n\nGather（汇集）→ Select（选择）→ Structure（结构化）→ Compress（压缩）\n\nGather：从多个来源汇集候选信息\n• 系统指令（最高优先级）\n• 记忆系统检索（MemoryTool）\n• RAG 系统检索（RAGTool）\n• 对话历史（保留最近 N 条）\n• 自定义信息包\n\nSelect：基于\"相关性 + 新近性\"加权评分\n• 分离系统指令和其他信息\n• 计算综合分数 = relevance_weight × 相关性 + recency_weight × 新近性\n• 贪心算法按分数填充，直到 token 上限" },
  { "type": "content", "title": "ContextBuilder 结构化输出", "content": "Structure 阶段将选中信息组织成结构化上下文模板：\n\n[Role & Policies]  ← 系统指令\n[Task]            ← 用户查询\n[Evidence]        ← RAG 检索的知识\n[Context]         ← 对话历史 + 记忆\n[Output]          ← 输出格式要求\n\nCompress 阶段在超限时执行兜底压缩：\n• 分区压缩：保持结构完整性\n• 按比例截断或 LLM 智能摘要\n• 确保系统指令始终有足够的预留空间（reserve_ratio）\n\n核心数据结构：\n• ContextPacket：候选信息包（内容、时间戳、token 数、相关性分数）\n• ContextConfig：配置管理（max_tokens、reserve_ratio、权重等）" },
  { "type": "code", "title": "滑动窗口上下文管理", "language": "python", "code": "from dataclasses import dataclass\nfrom datetime import datetime\nfrom typing import Optional, List\n\n@dataclass\nclass ContextPacket:\n    \"\"\"候选信息包\"\"\"\n    content: str\n    timestamp: datetime\n    token_count: int\n    relevance_score: float = 0.5\n    metadata: Optional[dict] = None\n\n    def __post_init__(self):\n        self.relevance_score = max(0.0, min(1.0, self.relevance_score))\n\nclass ContextBuilder:\n    \"\"\"上下文构建器 - GSSC 流水线\"\"\"\n    def __init__(self, config=None):\n        self.config = config or ContextConfig()\n\n    def build(self, user_query, conversation_history=None,\n              system_instructions=None, custom_packets=None):\n        # 1. Gather - 汇集信息\n        packets = self._gather(\n            user_query, conversation_history,\n            system_instructions, custom_packets\n        )\n\n        # 2. Select - 智能选择\n        total_tokens = self.config.max_tokens\n        reserve = int(total_tokens * self.config.reserve_ratio)\n        selected = self._select(packets, user_query, total_tokens - reserve)\n\n        # 3. Structure - 结构化输出\n        context = self._structure(selected, user_query)\n\n        # 4. Compress - 兜底压缩\n        if self.config.enable_compression:\n            context = self._compress(context, total_tokens)\n\n        return context\n\n    def _select(self, packets, query, budget):\n        \"\"\"基于相关性+新近性选择信息\"\"\"\n        scored = []\n        for p in packets:\n            relevance = self._calc_relevance(p.content, query)\n            recency = self._calc_recency(p.timestamp)\n            score = (\n                self.config.relevance_weight * relevance +\n                self.config.recency_weight * recency\n            )\n            if p.relevance_score >= self.config.min_relevance:\n                scored.append((score, p))\n\n        scored.sort(key=lambda x: x[0], reverse=True)\n\n        # 贪心选择\n        selected, tokens = [], 0\n        for _, p in scored:\n            if tokens + p.token_count <= budget:\n                selected.append(p)\n                tokens += p.token_count\n        return selected\n\n    def _calc_relevance(self, content, query):\n        \"\"\"Jaccard 相似度（可替换为向量相似度）\"\"\"\n        c_words = set(content.lower().split())\n        q_words = set(query.lower().split())\n        inter = c_words & q_words\n        union = c_words | q_words\n        return len(inter) / len(union) if union else 0.0\n\n    def _calc_recency(self, timestamp):\n        \"\"\"指数衰减新近性分数\"\"\"\n        import math\n        hours = (datetime.now() - timestamp).total_seconds() / 3600\n        return max(0.1, math.exp(-0.1 * hours / 24))", "explanation": "ContextBuilder 的核心是 GSSC 流水线。Select 阶段使用贪心算法，基于相关性和新近性的加权分数选择最有价值的信息。Compress 阶段在超限时执行分区压缩，确保在有限 token 预算内保留最高信号密度的上下文。" },
  { "type": "content", "title": "NoteTool：结构化笔记", "content": "NoteTool 是为\"长时程任务\"提供的结构化外部记忆组件。\n\n存储格式：Markdown + YAML 混合格式\n---\nid: note_20250119_153000_0\ntitle: 项目进展 - 第一阶段\ntype: task_state\ntags: [refactoring, phase1]\ncreated_at: 2025-01-19T15:30:00\nupdated_at: 2025-01-19T15:30:00\n---\n# 项目进展\n已完成数据模型层的重构...\n\n优势：\n• 结构化记录：YAML 元数据机器可解析，Markdown 正文人类可读\n• 版本友好：纯文本格式，天然支持 Git\n• 低开销：无需复杂数据库操作\n\n笔记类型：task_state / conclusion / blocker / action / reference" },
  { "type": "content", "title": "TerminalTool：即时文件系统访问", "content": "TerminalTool 实现了 9.2.2 节提到的\"即时（Just-in-time, JIT）上下文\"理念——智能体不需要预先加载所有文件，而是按需探索和检索。\n\n四层安全机制：\n1. 命令白名单：只允许安全的只读命令（ls、cat、grep、find 等）\n2. 工作目录限制（沙箱）：只能访问指定目录\n3. 超时控制：防止无限循环或资源耗尽\n4. 输出大小限制：防止内存溢出\n\n典型使用模式：\n• 探索式导航：ls → cd → find → cat\n• 数据文件分析：head → wc → cut | sort | uniq\n• 日志文件分析：tail | grep、awk\n• 代码库分析：find + wc、grep -rn" },
  { "type": "content", "title": "长程智能体实战：代码库维护助手", "content": "整合 ContextBuilder + NoteTool + TerminalTool + MemoryTool，构建跨会话的代码库维护助手。\n\n三层架构：\n• TerminalTool（即时访问）→ 按需探索代码\n• MemoryTool（会话记忆）→ 当前对话上下文\n• NoteTool（持久笔记）→ 跨会话状态追踪\n\n实际工作流：\n第1天：探索代码库\n  → 自动识别模块结构，记录关键发现\n\n第2天：分析代码质量\n  → 检测代码重复、复杂度、缺少测试等问题\n  → 自动创建 blocker 笔记\n\n第3天：规划重构任务\n  → 基于历史笔记制定优先级\n  → blocker → action → task_state 递进\n\n关键特性：跨会话连贯性、智能上下文管理、自动知识沉淀" },
  { "type": "content", "title": "本章小结", "content": "• 上下文工程从\"提示工程\"自然演进而来，核心是管理有限的注意力预算\n• 上下文腐蚀（Context Rot）是长上下文场景的关键挑战\n• GSSC 流水线（Gather → Select → Structure → Compress）提供统一的上下文管理框架\n• 三大策略：压缩整合、结构化笔记、子代理架构\n• NoteTool（Markdown + YAML）支持持久化、结构化的长期记忆\n• TerminalTool 实现 JIT 上下文，按需探索文件系统\n• 分层上下文管理（即时访问 + 会话记忆 + 持久笔记）是构建长程智能体的最佳实践" },
  { "type": "quiz", "title": "知识测验" }
]